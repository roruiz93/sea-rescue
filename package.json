{
  "name": "sea-rescue",
  "version": "1.0.0",
  "description": "2D Mobile Sea Rescue Game – Phaser 3 + Capacitor",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap sync android && npx cap open android",
    "cap:run": "npx cap run android",
    "generate-assets": "python tools/generate_assets.py",
    "questions": "python tools/question_manager.py"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "vite": "^5.2.0"
  }
}
